# MOVX production GLB pack

Drop the approved Tripo exports in this folder using these exact filenames:

- `movx-physical-logo.glb` → `hero-movx-logo`
- `movx-x-portal.glb` → `x-portal`
- `movx-creative-machine.glb` → `creative-machine`
- `movx-camera.glb` → `play-camera`
- `movx-x-cube.glb` → `play-cube`
- `movx-spatial-studio.glb` → `spatial-studio`

The v323 installer publishes only files that exist. Missing files keep the approved DOM/CSS fallback.
