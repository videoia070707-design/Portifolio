name: Deploy MOVX to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "MOVX restore staging"
